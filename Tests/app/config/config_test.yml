# Framework Configuration
framework:
    secret: "TOP-SECRET"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: %kernel.debug%
    templating:
        engines: ['twig']
    test: ~